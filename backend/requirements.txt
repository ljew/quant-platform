# 个人量化投研平台 — 后端依赖
# 架构（设计 v1.0 落地）：业务写表 SQLite + 分析读表 DuckDB；数据源 tushare/akshare（均 lazy import）

fastapi==0.115.6
uvicorn[standard]==0.34.0
sqlalchemy==2.0.36
pydantic==2.10.4
pydantic-settings==2.7.1
alembic==1.14.0
pandas==2.2.3
numpy==2.1.3
python-dateutil==2.9.0.post0
httpx==0.28.1
# DuckDB 分析数据层（回测/因子研究读数据列式加速）
duckdb==1.5.5
duckdb-engine==0.17.0
# 数据源（均 lazy import，未安装时平台仍可启动，仅数据接入功能不可用）
akshare==1.16.99
tushare==1.4.29
# datahub 数据枢纽（Bronze/Silver 分层存储 + 源配置）
pyarrow>=14.0
PyYAML>=6.0
