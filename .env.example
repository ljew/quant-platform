# 复制为 .env 后填写（docker compose 自动读取）
# Tushare token（指数成分股/行情兜底数据源）——必填，否则依赖 akshare 单数据源
QUANT_TUSHARE_TOKEN=
# 启用数据调度：每交易日 19:00 ETL 自动日更 + 断供自愈 + 指数成分(PIT)月度快照刷新
QUANT_DATA_SCHEDULE=1

# —— 因子挖掘「自然语言 → 表达式」（可选）——
# 【推荐】直接在页面上配置：因子挖掘页 → AI 生成因子 → 「配置模型」。
#   支持多家通道并存、切换默认、连通性测试，且**保存即生效，无需重启后端**。
# 本文件为兜底通道：仅当页面上一条通道都没配置时才会使用这里的值。
#   ⚠️ 页面上只要有一条通道，就会覆盖这里 —— 避免「改了 .env 却没反应」的困惑。
# 走 OpenAI 兼容协议，DeepSeek / 火山方舟(豆包) / DashScope(千问) 都适用。
# 两边都不配置时，功能降级为「本地关键词试用模式」，手写表达式不受影响。
QUANT_LLM_API_KEY=
# DeepSeek：https://api.deepseek.com/v1  模型 deepseek-chat
QUANT_LLM_BASE_URL=https://api.deepseek.com/v1
QUANT_LLM_MODEL=deepseek-chat
# 火山方舟(豆包)：https://ark.cn-beijing.volces.com/api/v3  模型填推理接入点 ID
# 通义千问：https://dashscope.aliyuncs.com/compatible-mode/v1  模型 qwen-plus
